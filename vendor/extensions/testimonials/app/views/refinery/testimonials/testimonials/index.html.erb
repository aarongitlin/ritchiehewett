<% content_for :body do %>
 <% unless @testimonials.blank? %>
   <article id="testimonials">
     <%= render :partial => "testimonial", :collection => @testimonials %>
   </article>
 <% end %>
<% end %>

<%= render "/refinery/content_page", :hide_sections => :body_content_title %>

  <div class="testi_pagination">
    <%= will_paginate @users, :renderer => BootstrapPagination::Rails, :previous_label => 'Previous', :next_label => 'Next' %>
  </div>
  <%= pageless(@testimonials.total_pages, @testimonials_path) %>
